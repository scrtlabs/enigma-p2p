const path = require('path');
const assert = require('assert');
const EnigmaContractProductionWriterAPI = require(path.join(__dirname, '../../src/ethereum/EnigmaContractProductionWriterAPI'));

describe('Wait for x confirmations', () => {
    describe('Writer', () => {
        it('register', async () => {

        })
        it("deposit", async () => { })
        it("selfDeposit", async () => { })
        it("withdraw", async () => { })
        it("deploySecretContract", async () => { })
        it("login", async () => { })
        it("logout", async () => { })
        it("commitReceipt", async () => { })
        it("commitTaskFailure", async () => { })
        it("deploySecretContractFailure", async () => { })
    })
})