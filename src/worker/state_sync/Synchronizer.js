
class Synchronizer{

    constructor(enigmaNode){
        this._enigmaNode = enigmaNode;
    }
    /** announce the network the CID's that are provided
     * @param {Array<EngCID>} cidList,
     * @param {Function} onDone, (erroredCID)=>{}
     * */
    provide(engCIDList, onDone){}

    /** */
}