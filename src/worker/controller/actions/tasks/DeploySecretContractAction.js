/**
 * This action takes a raw message (usually jsonrpc or self compute)
 * - turns it into a Task object
 * - sends to TaskManager
 * */
class DeploySecretContractAction{
  constructor(controller) {
    this._controller = controller;
  }
  execute(params){

  }
}
module.exports;
